(ns cube-test.db)

;; This is for defining what you want in "db" for all scenes.  Game level scene state
;; should be added in separately (cf. how 'msg-cube' does it.)
(def default-db
  {:name "re-frame"})

   ; :msgs []
   ; :msgs-2 []
   ; :ints [0 1]
   ; :max-id 0
   ; :input-id 2})
