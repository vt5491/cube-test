;; This is kind of a dummy user so proto-repl can work with shadow-cljs.
;; see https://shadow-cljs.github.io/docs/UsersGuide.html#_proto_repl_atom

; (ns cube-test.user)
(ns user)

(defn reset [])

; (println "hi")
; (js-debugger)
